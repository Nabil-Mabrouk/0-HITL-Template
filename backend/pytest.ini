[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no DB, no HTTP)
    integration: Integration tests (HTTP + SQLite in-memory)
addopts = -v --tb=short
