# Dépendances de test — ne pas inclure en production
# Installation : pip install -r requirements-test.txt

# Framework de test
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# Client HTTP pour tester FastAPI
httpx==0.27.2

# Base de données en mémoire pour les tests
# (SQLite via SQLAlchemy — sans dépendance PostgreSQL externe)

# Factories / fixtures de données
factory-boy==3.3.1
faker==30.8.2

# Assertions enrichies
pytest-clarity==1.0.1

# Rapport de couverture HTML
coverage[toml]==7.6.4
