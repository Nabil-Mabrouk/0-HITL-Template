/**
 * RecentServices — affiche les derniers services utilisés par l'utilisateur.
 * Placeholder à compléter selon les données disponibles dans l'API.
 */
export function RecentServices() {
  return (
    <div className="bg-white rounded-lg shadow p-6">
      <h2 className="text-xl font-bold text-gray-900 mb-4">Services récents</h2>
      <p className="text-gray-500 text-sm">Aucun service récent pour l'instant.</p>
    </div>
  );
}
