/**
 * Visual effect components — drop into any page section for instant personality.
 *
 * Background effects (absolutely positioned, use inside a relative container):
 *   GlowOrbs      — blurred radial blobs in brand colors  (dark sites)
 *   MeshGradient  — animated CSS gradient mesh            (dark or light)
 *   GridDots      — dot / line / cross grid pattern       (technical look)
 *   NoiseOverlay  — film grain texture                    (editorial, luxury)
 *
 * Decorator effects (wrap existing elements):
 *   AnimatedBorder — rotating conic gradient border      (CTAs, featured cards)
 */

export { GlowOrbs }      from './GlowOrbs'
export { MeshGradient }  from './MeshGradient'
export { GridDots }      from './GridDots'
export { NoiseOverlay }  from './NoiseOverlay'
export { AnimatedBorder } from './AnimatedBorder'
